# Sprint 1: Project Setup

## Goals
- Initialize git repo and GitHub remote
- Create package.json with n8n community node conventions
- Set up TypeScript, ESLint, build tooling
- Create project structure (directories, .gitignore, LICENSE)

## Tasks
- [ ] `git init`
- [ ] Create `package.json` (name: n8n-nodes-advanced-webhook-response, keywords: n8n-community-node-package)
- [ ] Create `tsconfig.json` (matching n8n-nodes-starter template)
- [ ] Create `.gitignore` (dist/, node_modules/)
- [ ] Create `LICENSE` (MIT)
- [ ] Create directory structure: `nodes/AdvancedRespondToWebhook/utils/`, `nodes/AdvancedRespondToWebhook/icons/`
- [ ] `npm install` dependencies
- [ ] Create GitHub repo and push initial commit

## Definition of Done
- `npm run build` runs (even if no source yet)
- Project structure matches plan
- GitHub repo created with initial commit
