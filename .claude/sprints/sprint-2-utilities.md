# Sprint 2: Utility Modules

## Goals
- Create iframe wrapping logic with HTML escaping
- Create header builder helpers (CORS, security, cookies)
- Create HTML auto-detection utility

## Tasks
- [ ] `utils/iframeWrapper.ts` — escapeForSrcdoc(), wrapInIframe()
- [ ] `utils/headerBuilders.ts` — buildCorsHeaders(), buildSecurityHeaders(), buildCookieHeaders()
- [ ] `utils/htmlDetection.ts` — looksLikeHtml()
- [ ] Verify build compiles all utils

## Definition of Done
- All utility functions implemented and exported
- `npm run build` succeeds
